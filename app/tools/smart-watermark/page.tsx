'use client';

import { SmartWatermarkTool } from '@/components/watermark/smart-watermark-tool';

export default function SmartWatermarkPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100">
      <SmartWatermarkTool />
    </div>
  );
}